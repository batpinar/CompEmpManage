@model IEnumerable<Company>
@{
    ViewData["Title"] = "Index";
}

<div class="container pb-5">

    <div class="row pt-4">
        <div class="col-6">
            <h2>Copmany List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp asp-controller="Company" asp-action="Create" class="btn btn-success text-black">
                <i class="bi bi-patch-plus"></i> &nbsp; Create New Company
            </a>
        </div>
    </div>


    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th class="text-center">
                    CompanyName
                </th>
                <th class="text-center">
                    TaxNumber
                </th>
                <th class="text-center">
                    TaxOffice
                </th>
                @*<th class="text-center">
                    Employee Count
                </th>*@
                <th class="text-center">
                    Adresses
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody @foreach (var obj in Model)
               {
            <tr>
                <td width="25%" class="text-center">
                    @obj.CompanyName
                </td>

                 <td width="10%" class="text-center">
                    @obj.TaxNumber
                </td>
                <td width="15%" class="text-center">
                    @obj.TaxOffice
                </td>
                @*<td width="10%" class="text-center">
                    @obj.EmployeeCount
                </td>*@
                <td width="40%" class="text-center">
                    @obj.Adresses
                </td>
                <td width="10%">

                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Company" asp-action="Edit" asp-route-id="@obj.Id"> <i class="bi bi-pencil"></i> Edit</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
    </table>


</div>

